a=10
b=10
#as two variables are same it will assighn the two variables at the same memory where 10 is stored unlike c
print(id(a))
print(id(b))
#to check if the two variables are assighned to the same memory or not
